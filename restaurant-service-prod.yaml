spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/restaurant_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 5051

info:
  app:
    name: restaurant-service
    description: Restaurant microservice for managing menus and orders
    version: 1.0.0 Production
  developer:
    name: Zohaib_Anwar
    email: zohaib584@gmail.com
